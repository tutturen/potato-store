sudo: required
services:
  - docker
language: python
python: "3.6"
install:
  - make install_dep
script:
  - make test
  - make docker_build_prod

deploy:
  provider: script
  script: make docker_push_prod
  on:
    branch: master

